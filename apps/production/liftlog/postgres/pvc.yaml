apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: liftlog-postgres-data
  namespace: liftlog
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
