---
# Opstree Redis Operator — manages RedisReplication + RedisSentinel CRDs
# Installs into its own namespace; creates cluster-wide CRDs + RBAC
apiVersion: v1
kind: Namespace
metadata:
  name: redis-operator-system
  labels:
    kubernetes.io/metadata.name: redis-operator-system
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-operator
  namespace: redis-operator-system
spec:
  interval: 2m
  chart:
    spec:
      chart: redis-operator
      version: "0.25.0"
      sourceRef:
        kind: HelmRepository
        name: ot-helm
        namespace: flux-system
      interval: 1h
  install:
    crds: CreateReplace
    remediation:
      retries: -1
  upgrade:
    crds: CreateReplace
    remediation:
      retries: -1
  values:
    # Operator watches all namespaces by default
    redisOperator:
      imagePullPolicy: IfNotPresent
