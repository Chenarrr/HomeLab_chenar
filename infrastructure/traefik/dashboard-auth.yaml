# This secret is NOT managed by Flux/git.
# Apply it manually once:
#
#   htpasswd -nb <user> <password> | base64
#   kubectl create secret generic traefik-dashboard-auth \
#     --from-literal=users='<htpasswd-line>' \
#     -n traefik
#
# Flux will not prune manually-created secrets (no flux.toolkit.fluxcd.io/prune label).
